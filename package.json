{
  "name": "doctor-portal-maker",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix client",
    "lint": "npm run lint --prefix client",
    "start:server": "npm start --prefix server"
  }
}
